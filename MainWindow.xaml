<Window x:Class="ScreenTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ScreenTimer"
        Title="TimeTimer" Height="120" Width="250"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border x:Name="MainBorder" Background="#222222" CornerRadius="15" BorderBrush="#00FF00" BorderThickness="2">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="lblTime" Text="00:00:00" FontSize="36" Foreground="White" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                <Button x:Name="btnToggle" Content="START" Click="btnToggle_Click" Width="60" Margin="5" Background="#444" Foreground="White"/>
                <Button x:Name="btnSync" Content="SYNC" Click="btnSync_Click" Width="60" Margin="5" Background="#444" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
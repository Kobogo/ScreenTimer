<Window x:Class="ScreenTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ScreenTimer" Height="120" Width="250"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        x:Name="ThisWindow"> <Border x:Name="MainBorder" Background="#222222" CornerRadius="15"
            BorderBrush="#00FF00" BorderThickness="2">
        <Grid>
            <Button x:Name="btnSizeToggle" Content="" FontFamily="Segoe MDL2 Assets"
                    FontSize="10" Background="Transparent" Foreground="Gray"
                    BorderThickness="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="8" Click="btnSizeToggle_Click" Cursor="Hand"/>

            <StackPanel x:Name="MainContainer" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="lblTime" Text="00:00:00" FontSize="36" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>

                <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <Button x:Name="btnToggle" Content="START" Click="btnToggle_Click" Width="65" Height="25" Margin="5" Background="#444" Foreground="White" BorderThickness="0"/>
                    <Button x:Name="btnSync" Content="SYNC" Click="btnSync_Click" Width="65" Height="25" Margin="5" Background="#444" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>